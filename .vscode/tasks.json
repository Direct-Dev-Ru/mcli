{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "echo Hello",
        },
        {
            "type": "shell",
            "label": "go: build package",
            "command": "go",
            "args": [
                "build",
                "${workspaceFolder}"
            ],
            "problemMatcher": [
                "$go"
            ],
            "group": "build",
            "detail": "cd /home/su/projects/golang/cobra-cli-example; go build ../${fileDirname}"
        }
    ]
}